# nixpacks.toml
[phases.setup]
nixpkgsArchive = "ffeebf0acf3ae8b29f8c7049cd911b9636efd7e7"   # optional, keep the same pin if you want reproducibility

[phases.setup]
# Force the exact Node version that satisfies Vite & your engines field
nixPkgs = ["nodejs-22.16.0"]

# (optional) also pin npm if you want a specific version
# nixPkgs = ["nodejs-22.16.0", "npm-10_x"]